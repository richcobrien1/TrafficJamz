[build]
builder = "heroku/buildpacks:20" # only if you're not using Docker

[env]
PORT = "5000"

[services]
  internal_port = 5000
  [[services.ports]]
    handlers = ["http"]
    port = 80
  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443