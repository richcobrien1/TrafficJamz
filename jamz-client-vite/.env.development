# Development environment variables
# Directly hit backend on port 5000 (bypasses nginx)
VITE_API_BASE=http://localhost:5000/api

# Optional: other dev settings
VITE_APP_ENV=development
VITE_APP_NAME=TrafficJamz
