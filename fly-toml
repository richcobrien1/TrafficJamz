app = "trafficjamz"
primary_region = "iad"

[build]
  builder = "heroku/buildpacks:20"

[[services]]
  internal_port = 3000
  processes = ["app"]
  protocol = "tcp"
  [[services.ports]]
    handlers = ["http"]
    port = 80
  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443